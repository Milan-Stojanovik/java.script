// test case 3


let names = ("Jocka", "Milan", "Pero", "Test12", "Test");
for (let i = 0; i < names.length; i++) {
    let length = names[i].length;
    if (length % 2 === 0)
        console.log(names[i]);
}

// This is where test case 3 ends